# Qwen3 14B + vLLM 项目依赖

# vLLM - 高性能推理引擎
vllm>=0.6.0

# Hugging Face 库
transformers>=4.40.0
huggingface-hub>=0.23.0
tokenizers>=0.19.0

# ModelScope - 魔搭社区（国内下载推荐）
modelscope>=1.11.0

# PyTorch (根据您的 CUDA 版本选择合适的版本)
# 以下是 CUDA 12.1 版本的示例
# torch>=2.3.0
# 如果您使用不同的 CUDA 版本，请访问 https://pytorch.org/get-started/locally/ 获取正确的安装命令

# 其他依赖
numpy>=1.24.0
accelerate>=0.30.0
sentencepiece>=0.2.0

# 可选：性能监控
# nvidia-ml-py3>=7.352.0
# psutil>=5.9.0

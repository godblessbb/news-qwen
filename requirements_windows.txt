# Qwen3 14B 项目依赖（Windows 版本）
# 注意：vLLM 不支持 Windows，此版本使用 transformers

# Hugging Face 库
transformers>=4.40.0
huggingface-hub>=0.23.0
tokenizers>=0.19.0

# ModelScope - 魔搭社区（国内下载推荐）
modelscope>=1.11.0

# PyTorch (需要手动安装)
# 请先运行：
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# 其他依赖
numpy>=1.24.0
accelerate>=0.30.0
sentencepiece>=0.2.0

# 量化支持（可选，用于节省显存）
bitsandbytes>=0.41.0

# 性能监控（可选）
# psutil>=5.9.0

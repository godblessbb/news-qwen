# Qwen3-14B 快速开始指南

> 本指南适用于已下载模型到D盘的用户

## 📋 环境准备

### 1️⃣ 创建 Conda 虚拟环境

打开命令行（CMD 或 PowerShell），执行以下命令：

```bash
# 创建名为 qwen3 的 Python 3.10 环境
conda create -n qwen3 python=3.10 -y

# 激活环境
conda activate qwen3
```

**提示**：以后每次使用前都需要先激活环境：
```bash
conda activate qwen3
```

### 2️⃣ 安装 PyTorch（重要！）

根据你的 CUDA 版本选择：

**CUDA 12.1+：**
```bash
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
```

**CUDA 11.8：**
```bash
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

**不确定 CUDA 版本？** 运行：
```bash
nvidia-smi
```
查看右上角的 CUDA Version

### 3️⃣ 安装项目依赖

**Windows 用户（推荐）：**
```bash
pip install -r requirements_windows.txt
```

**Linux 用户：**
```bash
pip install -r requirements.txt
```

### 4️⃣ 配置国内镜像源（可选，加速下载）

**Windows：**
```bash
setup_china_mirrors.bat
```

**Linux：**
```bash
./setup_china_mirrors.sh
```

---

## 🚀 运行模型

### 方式一：使用 transformers（推荐 Windows 用户）

#### 快速测试（单次问答）
```bash
python transformers_inference.py --model-path D:/models/qwen3-14b
```

#### 批量测试（5个示例问题）
```bash
python transformers_inference.py --model-path D:/models/qwen3-14b --batch-test
```

#### 交互式对话
```bash
python transformers_inference.py --model-path D:/models/qwen3-14b --interactive
```
输入 `exit` 或 `quit` 退出

#### 使用量化节省显存（如果显存不足）
```bash
# 8-bit 量化
python transformers_inference.py --model-path D:/models/qwen3-14b --load-in-8bit

# 4-bit 量化（显存需求最小）
python transformers_inference.py --model-path D:/models/qwen3-14b --load-in-4bit
```

---

### 方式二：使用 vLLM（推荐 Linux 用户，性能更高）

**注意**：vLLM 不支持 Windows，仅限 Linux 系统

#### 快速测试
```bash
python vllm_inference.py --model-path ~/models/qwen3-14b
```

#### 批量测试
```bash
python vllm_inference.py --model-path ~/models/qwen3-14b --batch-test
```

#### 交互模式
```bash
python vllm_inference.py --model-path ~/models/qwen3-14b --interactive
```

#### 多GPU加速（如果有多张显卡）
```bash
# 使用 2 张 GPU
python vllm_inference.py --model-path ~/models/qwen3-14b --tensor-parallel-size 2

# 使用 4 张 GPU
python vllm_inference.py --model-path ~/models/qwen3-14b --tensor-parallel-size 4
```

---

## 📊 批量推理示例

运行内置的批量推理示例（新闻分类、摘要生成、情感分析）：

```bash
python batch_inference_example.py --model-path D:/models/qwen3-14b --examples
```

处理自己的数据文件：

```bash
# 处理 txt 文件（每行一个问题）
python batch_inference_example.py --model-path D:/models/qwen3-14b --input-file questions.txt

# 处理 json 文件
python batch_inference_example.py --model-path D:/models/qwen3-14b --input-file data.json

# 自定义批量大小
python batch_inference_example.py --model-path D:/models/qwen3-14b --input-file data.txt --batch-size 16
```

---

## 🔍 验证安装

检查环境是否正确配置：

```python
# 运行 Python，输入以下代码
import torch
print(f"PyTorch 版本: {torch.__version__}")
print(f"CUDA 可用: {torch.cuda.is_available()}")
print(f"CUDA 版本: {torch.version.cuda}")
print(f"GPU 数量: {torch.cuda.device_count()}")
if torch.cuda.is_available():
    print(f"GPU 名称: {torch.cuda.get_device_name(0)}")
```

---

## 🎯 常见模型路径

根据你的系统和存放位置，常见路径包括：

| 系统 | 路径示例 |
|------|---------|
| Windows D盘 | `D:/models/qwen3-14b` |
| Windows E盘 | `E:/models/qwen3-14b` |
| Linux 主目录 | `~/models/qwen3-14b` |
| Linux 绝对路径 | `/home/username/models/qwen3-14b` |

**如果模型在其他位置**，使用 `--model-path` 参数指定完整路径。

---

## ⚠️ 常见问题

### 问题1：显存不足（CUDA out of memory）

**解决方案**：
1. 使用量化模型（推荐 4-bit）
   ```bash
   python transformers_inference.py --model-path D:/models/qwen3-14b --load-in-4bit
   ```

2. 减小批量大小
   ```bash
   python batch_inference_example.py --model-path D:/models/qwen3-14b --batch-size 4
   ```

### 问题2：找不到模型文件

**检查步骤**：
1. 确认模型路径是否正确
2. 检查目录中是否包含 `config.json` 文件：
   ```bash
   # Windows
   dir D:\models\qwen3-14b

   # Linux
   ls ~/models/qwen3-14b
   ```

### 问题3：conda activate 不工作

**解决方案**：
```bash
# 初始化 conda（仅需运行一次）
conda init

# 关闭并重新打开命令行窗口
# 然后重试
conda activate qwen3
```

---

## 📚 完整文档

- **中国镜像加速指南**：见 `CHINA_MIRROR_GUIDE.md`
- **Windows 系统专用方案**：见 `WINDOWS_SOLUTIONS.md`
- **完整 API 文档**：见 `README.md`

---

## 💡 快速参考

**最简单的测试命令（Windows）：**
```bash
conda activate qwen3
python transformers_inference.py --model-path D:/models/qwen3-14b --batch-test
```

**交互式对话（最常用）：**
```bash
conda activate qwen3
python transformers_inference.py --model-path D:/models/qwen3-14b --interactive
```

祝使用愉快！ 🎉
